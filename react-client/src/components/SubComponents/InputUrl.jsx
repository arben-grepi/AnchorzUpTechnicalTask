import { Input } from "@chakra-ui/react";

// eslint-disable-next-line react/prop-types
const InputUrl = ({ placeholder }) => {
  return <Input size={"2xl"} placeholder={placeholder} />;
};

export default InputUrl;
